# RazorGuard requirements
# Grouped by the day/component that first needs them (ARCHITECTURE.md Section 2).

# Day 1 — ingestion, canonicalization, pseudo-entity resolution, tests
pandas==2.2.2
numpy==1.26.4
pyyaml==6.0.1
pytest==8.2.0
pyarrow==17.0.0

# Day 1 (infra, once Step 5 of Day 1 passes) / Day 2 — API + DB
fastapi==0.111.0
uvicorn==0.29.0
pydantic==2.7.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Day 1/2 — graph construction (connected components only — no GNN/community detection)
networkx==3.3

# Day 2 — transaction risk model
xgboost==2.0.3
scikit-learn==1.4.2

# Day 2/3 — dashboard
streamlit==1.35.0

# Day 4 — Investigation Agent (install whichever provider you'll actually use)
# Pinned to versions confirmed to avoid the "Client.__init__() got an
# unexpected keyword argument 'proxies'" error — a real dependency conflict
# where older SDK versions pass a `proxies` kwarg to httpx that newer httpx
# releases removed.
anthropic==1.3.0
groq==1.7.0